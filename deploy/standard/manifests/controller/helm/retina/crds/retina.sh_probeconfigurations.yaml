---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: probeconfigurations.retina.sh
spec:
  group: retina.sh
  names:
    kind: ProbeConfiguration
    listKind: ProbeConfigurationList
    plural: probeconfigurations
    singular: probeconfiguration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targets
      name: Targets
      type: integer
    - jsonPath: .status.activeProbes
      name: Active
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProbeConfiguration is the Schema for the probeconfigurations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProbeConfigurationSpec defines the desired state of ProbeConfiguration
            properties:
              nodeSelector:
                additionalProperties:
                  type: string
                description: |-
                  NodeSelector restricts which nodes run probes (optional)
                  If not specified, all nodes with Retina agent will probe
                type: object
              targets:
                description: Targets is a list of endpoints to probe
                items:
                  description: ProbeTarget defines a single endpoint to probe
                  properties:
                    endpoint:
                      description: |-
                        Endpoint is the target URL or host:port to probe
                        Examples: "https://google.com", "example.com:443", "10.0.0.1:80"
                      type: string
                    expectedStatusCode:
                      description: 'ExpectedStatusCode for HTTP/HTTPS probes (default:
                        200)'
                      type: integer
                    headers:
                      additionalProperties:
                        type: string
                      description: Headers to include in HTTP/HTTPS requests
                      type: object
                    interval:
                      default: 10s
                      description: Interval is the time between probes
                      pattern: ^([0-9]+(\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$
                      type: string
                    name:
                      description: Name is a friendly name for this probe target
                      type: string
                    protocol:
                      default: HTTP
                      description: Protocol specifies the probe type (HTTP, HTTPS,
                        TCP)
                      enum:
                      - HTTP
                      - HTTPS
                      - TCP
                      type: string
                    timeout:
                      default: 5s
                      description: Timeout for each probe
                      pattern: ^([0-9]+(\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$
                      type: string
                  required:
                  - endpoint
                  - name
                  - protocol
                  type: object
                minItems: 1
                type: array
            required:
            - targets
            type: object
          status:
            description: ProbeConfigurationStatus defines the observed state of ProbeConfiguration
            properties:
              activeProbes:
                description: ActiveProbes is the number of currently active probe
                  routines
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the probe configuration's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastProbeTime:
                description: LastProbeTime is when the last probe was executed
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
